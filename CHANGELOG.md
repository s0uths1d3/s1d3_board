## 0.3.1 (2026-09-07)

本次更新带来全新的**桌面应用使用时长统计模块**，统一了统计类页面的时间范围切换体验，并修复了若干显示与交互问题。

### ✨ 新功能

- **新增「应用时长」模块（独立 Tab）**
  - Rust 侧监听前台窗口，按 30 秒分段结算，按应用聚合使用时长
  - 总时长 / 活跃时长 / 挂机时长三张汇总卡
  - 应用排行：按总时长降序，活跃与挂机两段叠加，并显示应用图标
  - 各应用时长占比环形图，悬停可查看时长与占比
  - **隐私优先**：默认关闭，需在「设置」或应用时长页手动开启后才会开始采集，可随时关闭
  - 跨平台实现：Windows / macOS / Linux 各有实现，其他平台回退为空实现
  - 新增 `app_usage`、`app_icons` 数据表与对应迁移

- **统一范围切换栏 RangeBar**
  - 从统计页抽出为共用组件，统计页与「应用时长」页均接入
  - 支持日 / 周 / 月 / 年 / 自定义，左右箭头切换上一阶段 / 下一阶段

- **剪贴板收藏项高亮显示**，与未收藏项明显区分

### 🐛 修复

- 修复「应用时长」占比环形图上下被裁切（SVG viewBox 未给 stroke 预留边距）
- 修复设置页分组选中状态跨挂载丢失、进入设置页时选中项闪跳
- `v-tip` 指令对 `undefined` 绑定值容错，避免悬停时报错
- i18n：`RangeBar` 文案键补漏并完成 snake_case 同步
- 移除 `useFormatDate` 的 `formatDate` 再导出，消除自动导入重名警告

### ♻️ 重构与优化

- Rust 代码模块化：`lib.rs` 拆分出 `commands.rs` / `app_usage/` / `migrations.rs`
- i18n 语言消息拆分至 `assets/lang/*.toml`，消息键全量 snake_case，标签英文键化
- 各模块内容边距与宽度统一由外壳提供，页面间距保持一致

### 📚 文档

- README 改为**英文默认版**，新增 `README.zh-CN.md` 中文版，两版顶部提供双语索引
- 补充「为什么做 / 是什么 / 核心功能」说明，并按模块精简要点

---

变更范围：`v0.3.0` → `0.3.1`，共 14 次提交、46 个文件
